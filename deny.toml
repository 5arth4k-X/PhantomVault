[licenses]
allow = [
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "MIT",
    "MIT-0",
    "ISC",
    "Unicode-3.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "Zlib",
    "OpenSSL",
]
unused-allowed-license = "allow"

[bans]
multiple-versions = "warn"
wildcards = "deny"
# windows-sys has two versions pulled in transitively by rpassword.
# This is a transitive dependency conflict we cannot resolve without
# forking rpassword. Skipping the duplicate warning for this crate.
skip = [
    { name = "windows-sys", reason = "rpassword pulls two versions transitively" },
]

[advisories]
db-path = "~/.cargo/advisory-db"
db-urls = ["https://github.com/rustsec/advisory-db"]
version = 2
ignore = [
    "RUSTSEC-2024-0382",
    "RUSTSEC-2024-0373",
]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
